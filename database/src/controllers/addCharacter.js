/* import * as url from 'url';
import { response } from '../../../characters/src/utils';
import * as res from 'express/lib/response';
const {model}= req.param;
const record ={
_id,name,height,mass,hair_color,skin_color,eye_color,birth_year,gender,homeworld,films} = req.body;

constnewRecord = await store[model].insert(record);
response ( res, 201, newRecord);

 */
